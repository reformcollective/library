diff --git a/dist/compiled/react-dom/cjs/react-dom-client.development.js b/dist/compiled/react-dom/cjs/react-dom-client.development.js
index b9c75f2156c75d6ab171e09e0c58f5f9329aa4b8..06162bf09b80a5f4c61ac977b9729bfbd3dbb71f 100644
--- a/dist/compiled/react-dom/cjs/react-dom-client.development.js
+++ b/dist/compiled/react-dom/cjs/react-dom-client.development.js
@@ -20042,34 +20042,99 @@
     function getScriptSelectorFromKey(key) {
       return "script[async]" + key;
     }
+    var insertedHrefs = new Set();
+    var rulesByPrecedence = new Map();
+    var precedenceOrder = [];
+    var styleElement = null;
+    
+    function getOrCreateStyleElement() {
+      if (!styleElement) {
+        styleElement = document.createElement('style');
+        document.head.appendChild(styleElement);
+      }
+      return styleElement;
+    }
+    
+    function getInsertionIndex(sheet, precedence) {
+      const precedenceIndex = precedenceOrder.indexOf(precedence);
+      if (precedenceIndex === -1) {
+        // New precedence goes at the end
+        return sheet.cssRules.length;
+      }
+    
+      // Find the first rule index of the next precedence level
+      for (let i = precedenceIndex + 1; i < precedenceOrder.length; i++) {
+        const nextPrecedenceRules = rulesByPrecedence.get(precedenceOrder[i]);
+        if (nextPrecedenceRules?.[0]) {
+          return nextPrecedenceRules[0].ruleIndex;
+        }
+      }
+    
+      // If no next precedence found, insert at the end
+      return sheet.cssRules.length;
+    }
+    
+    function updateRuleIndices(startIndex, shift) {
+      // Update all rule indices that come after the insertion point
+      for (const rules of rulesByPrecedence.values()) {
+        for (const rule of rules) {
+          if (rule.ruleIndex >= startIndex) {
+            rule.ruleIndex += shift;
+          }
+        }
+      }
+    }
+    
+    function insertStyleTag(el, props, hoistableRoot) {
+      const sheet = el.sheet;
+      const precedence = props['data-precedence'];
+      const href = props['data-href'];
+      const insertionIndex = getInsertionIndex(el.sheet, precedence);
+      try {
+        // Insert the new rule at the calculated index
+        const ruleIndex = sheet.insertRule(props.children, insertionIndex);
+    
+        // Update indices of subsequent rules
+        updateRuleIndices(insertionIndex, 1);
+    
+        // Get or create array for this precedence
+        const rulesForPrecedence = rulesByPrecedence.get(precedence) || [];
+        rulesByPrecedence.set(precedence, rulesForPrecedence);
+    
+        // Add the new rule with its index
+        rulesForPrecedence.push({ href, ruleIndex });
+      } catch (error) {
+        console.error('Error inserting CSS rule:', error);
+      }
+      performance.mark('Style_end', {
+        detail: href
+      });
+    }
     function acquireResource(hoistableRoot, resource, props) {
       resource.count++;
       if (null === resource.instance)
         switch (resource.type) {
           case "style":
-            var instance = hoistableRoot.querySelector(
-              'style[data-href~="' +
-                escapeSelectorAttributeValueInsideDoubleQuotes(props.href) +
-                '"]'
-            );
-            if (instance)
-              return (
-                (resource.instance = instance),
-                markNodeAsHoistable(instance),
-                instance
-              );
+            var instance = getOrCreateStyleElement();
+            var hasHoistedHref = insertedHrefs.has(props.href);
+            if (!precedenceOrder.includes(props.precedence)) {
+              precedenceOrder.push(props.precedence);
+            }
+            if (hasHoistedHref) {
+                return (
+                  (resource.instance = instance),
+                  markNodeAsHoistable(instance),
+                  instance
+                );
+            }
             var styleProps = assign({}, props, {
               "data-href": props.href,
               "data-precedence": props.precedence,
               href: null,
               precedence: null
             });
-            instance = (
-              hoistableRoot.ownerDocument || hoistableRoot
-            ).createElement("style");
             markNodeAsHoistable(instance);
-            setInitialProperties(instance, "style", styleProps);
-            insertStylesheet(instance, props.precedence, hoistableRoot);
+            insertStyleTag(instance, styleProps, hoistableRoot);
             return (resource.instance = instance);
           case "stylesheet":
             styleProps = getStyleKey(props.href);
diff --git a/dist/compiled/react-dom/cjs/react-dom-client.production.js b/dist/compiled/react-dom/cjs/react-dom-client.production.js
index c83517e72b4ab07823315383e151f4d43ac6b4fe..8c08fd3aa9f84b7a7a7f921d8a3065b1bccb0297 100644
--- a/dist/compiled/react-dom/cjs/react-dom-client.production.js
+++ b/dist/compiled/react-dom/cjs/react-dom-client.production.js
@@ -14233,34 +14233,99 @@ function getScriptKey(src) {
 function getScriptSelectorFromKey(key) {
   return "script[async]" + key;
 }
+var insertedHrefs = new Set();
+var rulesByPrecedence = new Map();
+var precedenceOrder = [];
+var styleElement = null;
+
+function getOrCreateStyleElement() {
+  if (!styleElement) {
+    styleElement = document.createElement('style');
+    document.head.appendChild(styleElement);
+  }
+  return styleElement;
+}
+
+function getInsertionIndex(sheet, precedence) {
+  const precedenceIndex = precedenceOrder.indexOf(precedence);
+  if (precedenceIndex === -1) {
+    // New precedence goes at the end
+    return sheet.cssRules.length;
+  }
+
+  // Find the first rule index of the next precedence level
+  for (let i = precedenceIndex + 1; i < precedenceOrder.length; i++) {
+    const nextPrecedenceRules = rulesByPrecedence.get(precedenceOrder[i]);
+    if (nextPrecedenceRules?.[0]) {
+      return nextPrecedenceRules[0].ruleIndex;
+    }
+  }
+
+  // If no next precedence found, insert at the end
+  return sheet.cssRules.length;
+}
+
+function updateRuleIndices(startIndex, shift) {
+  // Update all rule indices that come after the insertion point
+  for (const rules of rulesByPrecedence.values()) {
+    for (const rule of rules) {
+      if (rule.ruleIndex >= startIndex) {
+        rule.ruleIndex += shift;
+      }
+    }
+  }
+}
+
+function insertStyleTag(el, props, hoistableRoot) {
+  const sheet = el.sheet;
+  const precedence = props['data-precedence'];
+  const href = props['data-href'];
+  const insertionIndex = getInsertionIndex(el.sheet, precedence);
+  try {
+    // Insert the new rule at the calculated index
+    const ruleIndex = sheet.insertRule(props.children, insertionIndex);
+
+    // Update indices of subsequent rules
+    updateRuleIndices(insertionIndex, 1);
+
+    // Get or create array for this precedence
+    const rulesForPrecedence = rulesByPrecedence.get(precedence) || [];
+    rulesByPrecedence.set(precedence, rulesForPrecedence);
+
+    // Add the new rule with its index
+    rulesForPrecedence.push({ href, ruleIndex });
+  } catch (error) {
+    console.error('Error inserting CSS rule:', error);
+  }
+  performance.mark('Style_end', {
+    detail: href
+  });
+}
 function acquireResource(hoistableRoot, resource, props) {
   resource.count++;
   if (null === resource.instance)
     switch (resource.type) {
       case "style":
-        var instance = hoistableRoot.querySelector(
-          'style[data-href~="' +
-            escapeSelectorAttributeValueInsideDoubleQuotes(props.href) +
-            '"]'
-        );
-        if (instance)
-          return (
-            (resource.instance = instance),
-            markNodeAsHoistable(instance),
-            instance
-          );
+        var instance = getOrCreateStyleElement();
+        var hasHoistedHref = insertedHrefs.has(props.href);
+        if (!precedenceOrder.includes(props.precedence)) {
+          precedenceOrder.push(props.precedence);
+        }
+        if (hasHoistedHref) {
+            return (
+              (resource.instance = instance),
+              markNodeAsHoistable(instance),
+              instance
+            );
+        }
         var styleProps = assign({}, props, {
           "data-href": props.href,
           "data-precedence": props.precedence,
           href: null,
           precedence: null
         });
-        instance = (hoistableRoot.ownerDocument || hoistableRoot).createElement(
-          "style"
-        );
         markNodeAsHoistable(instance);
-        setInitialProperties(instance, "style", styleProps);
-        insertStylesheet(instance, props.precedence, hoistableRoot);
+        insertStyleTag(instance, styleProps, hoistableRoot);
         return (resource.instance = instance);
       case "stylesheet":
         styleProps = getStyleKey(props.href);
diff --git a/dist/compiled/react-dom/cjs/react-dom-profiling.development.js b/dist/compiled/react-dom/cjs/react-dom-profiling.development.js
index 51f031a33573944e571e849185c317a9ac852e6d..ca206780800c3930fbbd382f920c246c74963c71 100644
--- a/dist/compiled/react-dom/cjs/react-dom-profiling.development.js
+++ b/dist/compiled/react-dom/cjs/react-dom-profiling.development.js
@@ -20054,34 +20054,99 @@
     function getScriptSelectorFromKey(key) {
       return "script[async]" + key;
     }
+    var insertedHrefs = new Set();
+    var rulesByPrecedence = new Map();
+    var precedenceOrder = [];
+    var styleElement = null;
+    
+    function getOrCreateStyleElement() {
+      if (!styleElement) {
+        styleElement = document.createElement('style');
+        document.head.appendChild(styleElement);
+      }
+      return styleElement;
+    }
+    
+    function getInsertionIndex(sheet, precedence) {
+      const precedenceIndex = precedenceOrder.indexOf(precedence);
+      if (precedenceIndex === -1) {
+        // New precedence goes at the end
+        return sheet.cssRules.length;
+      }
+    
+      // Find the first rule index of the next precedence level
+      for (let i = precedenceIndex + 1; i < precedenceOrder.length; i++) {
+        const nextPrecedenceRules = rulesByPrecedence.get(precedenceOrder[i]);
+        if (nextPrecedenceRules?.[0]) {
+          return nextPrecedenceRules[0].ruleIndex;
+        }
+      }
+    
+      // If no next precedence found, insert at the end
+      return sheet.cssRules.length;
+    }
+    
+    function updateRuleIndices(startIndex, shift) {
+      // Update all rule indices that come after the insertion point
+      for (const rules of rulesByPrecedence.values()) {
+        for (const rule of rules) {
+          if (rule.ruleIndex >= startIndex) {
+            rule.ruleIndex += shift;
+          }
+        }
+      }
+    }
+    
+    function insertStyleTag(el, props, hoistableRoot) {
+      const sheet = el.sheet;
+      const precedence = props['data-precedence'];
+      const href = props['data-href'];
+      const insertionIndex = getInsertionIndex(el.sheet, precedence);
+      try {
+        // Insert the new rule at the calculated index
+        const ruleIndex = sheet.insertRule(props.children, insertionIndex);
+    
+        // Update indices of subsequent rules
+        updateRuleIndices(insertionIndex, 1);
+    
+        // Get or create array for this precedence
+        const rulesForPrecedence = rulesByPrecedence.get(precedence) || [];
+        rulesByPrecedence.set(precedence, rulesForPrecedence);
+    
+        // Add the new rule with its index
+        rulesForPrecedence.push({ href, ruleIndex });
+      } catch (error) {
+        console.error('Error inserting CSS rule:', error);
+      }
+      performance.mark('Style_end', {
+        detail: href
+      });
+    }
     function acquireResource(hoistableRoot, resource, props) {
       resource.count++;
       if (null === resource.instance)
         switch (resource.type) {
           case "style":
-            var instance = hoistableRoot.querySelector(
-              'style[data-href~="' +
-                escapeSelectorAttributeValueInsideDoubleQuotes(props.href) +
-                '"]'
-            );
-            if (instance)
-              return (
-                (resource.instance = instance),
-                markNodeAsHoistable(instance),
-                instance
-              );
+            var instance = getOrCreateStyleElement();
+            var hasHoistedHref = insertedHrefs.has(props.href);
+            if (!precedenceOrder.includes(props.precedence)) {
+              precedenceOrder.push(props.precedence);
+            }
+            if (hasHoistedHref) {
+                return (
+                  (resource.instance = instance),
+                  markNodeAsHoistable(instance),
+                  instance
+                );
+            }
             var styleProps = assign({}, props, {
               "data-href": props.href,
               "data-precedence": props.precedence,
               href: null,
               precedence: null
             });
-            instance = (
-              hoistableRoot.ownerDocument || hoistableRoot
-            ).createElement("style");
             markNodeAsHoistable(instance);
-            setInitialProperties(instance, "style", styleProps);
-            insertStylesheet(instance, props.precedence, hoistableRoot);
+            insertStyleTag(instance, styleProps, hoistableRoot);
             return (resource.instance = instance);
           case "stylesheet":
             styleProps = getStyleKey(props.href);
diff --git a/dist/compiled/react-dom/cjs/react-dom-profiling.profiling.js b/dist/compiled/react-dom/cjs/react-dom-profiling.profiling.js
index ccad6c1dad229a2dc6d887a28f458409183dcfce..fe3937b595fd3537aaf44de9c2afd77632a90562 100644
--- a/dist/compiled/react-dom/cjs/react-dom-profiling.profiling.js
+++ b/dist/compiled/react-dom/cjs/react-dom-profiling.profiling.js
@@ -14862,34 +14862,99 @@ function getScriptKey(src) {
 function getScriptSelectorFromKey(key) {
   return "script[async]" + key;
 }
+var insertedHrefs = new Set();
+var rulesByPrecedence = new Map();
+var precedenceOrder = [];
+var styleElement = null;
+
+function getOrCreateStyleElement() {
+  if (!styleElement) {
+    styleElement = document.createElement('style');
+    document.head.appendChild(styleElement);
+  }
+  return styleElement;
+}
+
+function getInsertionIndex(sheet, precedence) {
+  const precedenceIndex = precedenceOrder.indexOf(precedence);
+  if (precedenceIndex === -1) {
+    // New precedence goes at the end
+    return sheet.cssRules.length;
+  }
+
+  // Find the first rule index of the next precedence level
+  for (let i = precedenceIndex + 1; i < precedenceOrder.length; i++) {
+    const nextPrecedenceRules = rulesByPrecedence.get(precedenceOrder[i]);
+    if (nextPrecedenceRules?.[0]) {
+      return nextPrecedenceRules[0].ruleIndex;
+    }
+  }
+
+  // If no next precedence found, insert at the end
+  return sheet.cssRules.length;
+}
+
+function updateRuleIndices(startIndex, shift) {
+  // Update all rule indices that come after the insertion point
+  for (const rules of rulesByPrecedence.values()) {
+    for (const rule of rules) {
+      if (rule.ruleIndex >= startIndex) {
+        rule.ruleIndex += shift;
+      }
+    }
+  }
+}
+
+function insertStyleTag(el, props, hoistableRoot) {
+  const sheet = el.sheet;
+  const precedence = props['data-precedence'];
+  const href = props['data-href'];
+  const insertionIndex = getInsertionIndex(el.sheet, precedence);
+  try {
+    // Insert the new rule at the calculated index
+    const ruleIndex = sheet.insertRule(props.children, insertionIndex);
+
+    // Update indices of subsequent rules
+    updateRuleIndices(insertionIndex, 1);
+
+    // Get or create array for this precedence
+    const rulesForPrecedence = rulesByPrecedence.get(precedence) || [];
+    rulesByPrecedence.set(precedence, rulesForPrecedence);
+
+    // Add the new rule with its index
+    rulesForPrecedence.push({ href, ruleIndex });
+  } catch (error) {
+    console.error('Error inserting CSS rule:', error);
+  }
+  performance.mark('Style_end', {
+    detail: href
+  });
+}
 function acquireResource(hoistableRoot, resource, props) {
   resource.count++;
   if (null === resource.instance)
     switch (resource.type) {
       case "style":
-        var instance = hoistableRoot.querySelector(
-          'style[data-href~="' +
-            escapeSelectorAttributeValueInsideDoubleQuotes(props.href) +
-            '"]'
-        );
-        if (instance)
-          return (
-            (resource.instance = instance),
-            markNodeAsHoistable(instance),
-            instance
-          );
+        var instance = getOrCreateStyleElement();
+        var hasHoistedHref = insertedHrefs.has(props.href);
+        if (!precedenceOrder.includes(props.precedence)) {
+          precedenceOrder.push(props.precedence);
+        }
+        if (hasHoistedHref) {
+            return (
+              (resource.instance = instance),
+              markNodeAsHoistable(instance),
+              instance
+            );
+        }
         var styleProps = assign({}, props, {
           "data-href": props.href,
           "data-precedence": props.precedence,
           href: null,
           precedence: null
         });
-        instance = (hoistableRoot.ownerDocument || hoistableRoot).createElement(
-          "style"
-        );
         markNodeAsHoistable(instance);
-        setInitialProperties(instance, "style", styleProps);
-        insertStylesheet(instance, props.precedence, hoistableRoot);
+        insertStyleTag(instance, styleProps, hoistableRoot);
         return (resource.instance = instance);
       case "stylesheet":
         styleProps = getStyleKey(props.href);
diff --git a/dist/compiled/react-dom-experimental/cjs/react-dom-client.development.js b/dist/compiled/react-dom-experimental/cjs/react-dom-client.development.js
index 4f33bd1fc9df4d7aab9a25a2a7f3ab98a2a21006..0061b3c2c5ee5470b9dad0ceeb3e26df81dac4f4 100644
--- a/dist/compiled/react-dom-experimental/cjs/react-dom-client.development.js
+++ b/dist/compiled/react-dom-experimental/cjs/react-dom-client.development.js
@@ -22852,34 +22852,99 @@
     function getScriptSelectorFromKey(key) {
       return "script[async]" + key;
     }
+    var insertedHrefs = new Set();
+    var rulesByPrecedence = new Map();
+    var precedenceOrder = [];
+    var styleElement = null;
+    
+    function getOrCreateStyleElement() {
+      if (!styleElement) {
+        styleElement = document.createElement('style');
+        document.head.appendChild(styleElement);
+      }
+      return styleElement;
+    }
+    
+    function getInsertionIndex(sheet, precedence) {
+      const precedenceIndex = precedenceOrder.indexOf(precedence);
+      if (precedenceIndex === -1) {
+        // New precedence goes at the end
+        return sheet.cssRules.length;
+      }
+    
+      // Find the first rule index of the next precedence level
+      for (let i = precedenceIndex + 1; i < precedenceOrder.length; i++) {
+        const nextPrecedenceRules = rulesByPrecedence.get(precedenceOrder[i]);
+        if (nextPrecedenceRules?.[0]) {
+          return nextPrecedenceRules[0].ruleIndex;
+        }
+      }
+    
+      // If no next precedence found, insert at the end
+      return sheet.cssRules.length;
+    }
+    
+    function updateRuleIndices(startIndex, shift) {
+      // Update all rule indices that come after the insertion point
+      for (const rules of rulesByPrecedence.values()) {
+        for (const rule of rules) {
+          if (rule.ruleIndex >= startIndex) {
+            rule.ruleIndex += shift;
+          }
+        }
+      }
+    }
+    
+    function insertStyleTag(el, props, hoistableRoot) {
+      const sheet = el.sheet;
+      const precedence = props['data-precedence'];
+      const href = props['data-href'];
+      const insertionIndex = getInsertionIndex(el.sheet, precedence);
+      try {
+        // Insert the new rule at the calculated index
+        const ruleIndex = sheet.insertRule(props.children, insertionIndex);
+    
+        // Update indices of subsequent rules
+        updateRuleIndices(insertionIndex, 1);
+    
+        // Get or create array for this precedence
+        const rulesForPrecedence = rulesByPrecedence.get(precedence) || [];
+        rulesByPrecedence.set(precedence, rulesForPrecedence);
+    
+        // Add the new rule with its index
+        rulesForPrecedence.push({ href, ruleIndex });
+      } catch (error) {
+        console.error('Error inserting CSS rule:', error);
+      }
+      performance.mark('Style_end', {
+        detail: href
+      });
+    }
     function acquireResource(hoistableRoot, resource, props) {
       resource.count++;
       if (null === resource.instance)
         switch (resource.type) {
           case "style":
-            var instance = hoistableRoot.querySelector(
-              'style[data-href~="' +
-                escapeSelectorAttributeValueInsideDoubleQuotes(props.href) +
-                '"]'
-            );
-            if (instance)
-              return (
-                (resource.instance = instance),
-                markNodeAsHoistable(instance),
-                instance
-              );
+            var instance = getOrCreateStyleElement();
+            var hasHoistedHref = insertedHrefs.has(props.href);
+            if (!precedenceOrder.includes(props.precedence)) {
+              precedenceOrder.push(props.precedence);
+            }
+            if (hasHoistedHref) {
+                return (
+                  (resource.instance = instance),
+                  markNodeAsHoistable(instance),
+                  instance
+                );
+            }
             var styleProps = assign({}, props, {
               "data-href": props.href,
               "data-precedence": props.precedence,
               href: null,
               precedence: null
             });
-            instance = (
-              hoistableRoot.ownerDocument || hoistableRoot
-            ).createElement("style");
             markNodeAsHoistable(instance);
-            setInitialProperties(instance, "style", styleProps);
-            insertStylesheet(instance, props.precedence, hoistableRoot);
+            insertStyleTag(instance, styleProps, hoistableRoot);
             return (resource.instance = instance);
           case "stylesheet":
             styleProps = getStyleKey(props.href);
diff --git a/dist/compiled/react-dom-experimental/cjs/react-dom-client.production.js b/dist/compiled/react-dom-experimental/cjs/react-dom-client.production.js
index ebb4594d5388ffb4361932658ea491dcd4acd8f8..80ac3f15ea6834c35220f5fc1dcfb88369ff344c 100644
--- a/dist/compiled/react-dom-experimental/cjs/react-dom-client.production.js
+++ b/dist/compiled/react-dom-experimental/cjs/react-dom-client.production.js
@@ -16209,34 +16209,99 @@ function getScriptKey(src) {
 function getScriptSelectorFromKey(key) {
   return "script[async]" + key;
 }
+var insertedHrefs = new Set();
+var rulesByPrecedence = new Map();
+var precedenceOrder = [];
+var styleElement = null;
+
+function getOrCreateStyleElement() {
+  if (!styleElement) {
+    styleElement = document.createElement('style');
+    document.head.appendChild(styleElement);
+  }
+  return styleElement;
+}
+
+function getInsertionIndex(sheet, precedence) {
+  const precedenceIndex = precedenceOrder.indexOf(precedence);
+  if (precedenceIndex === -1) {
+    // New precedence goes at the end
+    return sheet.cssRules.length;
+  }
+
+  // Find the first rule index of the next precedence level
+  for (let i = precedenceIndex + 1; i < precedenceOrder.length; i++) {
+    const nextPrecedenceRules = rulesByPrecedence.get(precedenceOrder[i]);
+    if (nextPrecedenceRules?.[0]) {
+      return nextPrecedenceRules[0].ruleIndex;
+    }
+  }
+
+  // If no next precedence found, insert at the end
+  return sheet.cssRules.length;
+}
+
+function updateRuleIndices(startIndex, shift) {
+  // Update all rule indices that come after the insertion point
+  for (const rules of rulesByPrecedence.values()) {
+    for (const rule of rules) {
+      if (rule.ruleIndex >= startIndex) {
+        rule.ruleIndex += shift;
+      }
+    }
+  }
+}
+
+function insertStyleTag(el, props, hoistableRoot) {
+  const sheet = el.sheet;
+  const precedence = props['data-precedence'];
+  const href = props['data-href'];
+  const insertionIndex = getInsertionIndex(el.sheet, precedence);
+  try {
+    // Insert the new rule at the calculated index
+    const ruleIndex = sheet.insertRule(props.children, insertionIndex);
+
+    // Update indices of subsequent rules
+    updateRuleIndices(insertionIndex, 1);
+
+    // Get or create array for this precedence
+    const rulesForPrecedence = rulesByPrecedence.get(precedence) || [];
+    rulesByPrecedence.set(precedence, rulesForPrecedence);
+
+    // Add the new rule with its index
+    rulesForPrecedence.push({ href, ruleIndex });
+  } catch (error) {
+    console.error('Error inserting CSS rule:', error);
+  }
+  performance.mark('Style_end', {
+    detail: href
+  });
+}
 function acquireResource(hoistableRoot, resource, props) {
   resource.count++;
   if (null === resource.instance)
     switch (resource.type) {
       case "style":
-        var instance = hoistableRoot.querySelector(
-          'style[data-href~="' +
-            escapeSelectorAttributeValueInsideDoubleQuotes(props.href) +
-            '"]'
-        );
-        if (instance)
-          return (
-            (resource.instance = instance),
-            markNodeAsHoistable(instance),
-            instance
-          );
+        var instance = getOrCreateStyleElement();
+        var hasHoistedHref = insertedHrefs.has(props.href);
+        if (!precedenceOrder.includes(props.precedence)) {
+          precedenceOrder.push(props.precedence);
+        }
+        if (hasHoistedHref) {
+            return (
+              (resource.instance = instance),
+              markNodeAsHoistable(instance),
+              instance
+            );
+        }
         var styleProps = assign({}, props, {
           "data-href": props.href,
           "data-precedence": props.precedence,
           href: null,
           precedence: null
         });
-        instance = (hoistableRoot.ownerDocument || hoistableRoot).createElement(
-          "style"
-        );
         markNodeAsHoistable(instance);
-        setInitialProperties(instance, "style", styleProps);
-        insertStylesheet(instance, props.precedence, hoistableRoot);
+        insertStyleTag(instance, styleProps, hoistableRoot);
         return (resource.instance = instance);
       case "stylesheet":
         styleProps = getStyleKey(props.href);
diff --git a/dist/compiled/react-dom-experimental/cjs/react-dom-profiling.development.js b/dist/compiled/react-dom-experimental/cjs/react-dom-profiling.development.js
index 0c9dd0fefa5edb404381ce2ca2fa713eab083fa3..73bbf47bedab6b1c1fee7f7a22a1c626429794c2 100644
--- a/dist/compiled/react-dom-experimental/cjs/react-dom-profiling.development.js
+++ b/dist/compiled/react-dom-experimental/cjs/react-dom-profiling.development.js
@@ -22860,34 +22860,99 @@
     function getScriptSelectorFromKey(key) {
       return "script[async]" + key;
     }
+    var insertedHrefs = new Set();
+    var rulesByPrecedence = new Map();
+    var precedenceOrder = [];
+    var styleElement = null;
+    
+    function getOrCreateStyleElement() {
+      if (!styleElement) {
+        styleElement = document.createElement('style');
+        document.head.appendChild(styleElement);
+      }
+      return styleElement;
+    }
+    
+    function getInsertionIndex(sheet, precedence) {
+      const precedenceIndex = precedenceOrder.indexOf(precedence);
+      if (precedenceIndex === -1) {
+        // New precedence goes at the end
+        return sheet.cssRules.length;
+      }
+    
+      // Find the first rule index of the next precedence level
+      for (let i = precedenceIndex + 1; i < precedenceOrder.length; i++) {
+        const nextPrecedenceRules = rulesByPrecedence.get(precedenceOrder[i]);
+        if (nextPrecedenceRules?.[0]) {
+          return nextPrecedenceRules[0].ruleIndex;
+        }
+      }
+    
+      // If no next precedence found, insert at the end
+      return sheet.cssRules.length;
+    }
+    
+    function updateRuleIndices(startIndex, shift) {
+      // Update all rule indices that come after the insertion point
+      for (const rules of rulesByPrecedence.values()) {
+        for (const rule of rules) {
+          if (rule.ruleIndex >= startIndex) {
+            rule.ruleIndex += shift;
+          }
+        }
+      }
+    }
+    
+    function insertStyleTag(el, props, hoistableRoot) {
+      const sheet = el.sheet;
+      const precedence = props['data-precedence'];
+      const href = props['data-href'];
+      const insertionIndex = getInsertionIndex(el.sheet, precedence);
+      try {
+        // Insert the new rule at the calculated index
+        const ruleIndex = sheet.insertRule(props.children, insertionIndex);
+    
+        // Update indices of subsequent rules
+        updateRuleIndices(insertionIndex, 1);
+    
+        // Get or create array for this precedence
+        const rulesForPrecedence = rulesByPrecedence.get(precedence) || [];
+        rulesByPrecedence.set(precedence, rulesForPrecedence);
+    
+        // Add the new rule with its index
+        rulesForPrecedence.push({ href, ruleIndex });
+      } catch (error) {
+        console.error('Error inserting CSS rule:', error);
+      }
+      performance.mark('Style_end', {
+        detail: href
+      });
+    }
     function acquireResource(hoistableRoot, resource, props) {
       resource.count++;
       if (null === resource.instance)
         switch (resource.type) {
           case "style":
-            var instance = hoistableRoot.querySelector(
-              'style[data-href~="' +
-                escapeSelectorAttributeValueInsideDoubleQuotes(props.href) +
-                '"]'
-            );
-            if (instance)
-              return (
-                (resource.instance = instance),
-                markNodeAsHoistable(instance),
-                instance
-              );
+            var instance = getOrCreateStyleElement();
+            var hasHoistedHref = insertedHrefs.has(props.href);
+            if (!precedenceOrder.includes(props.precedence)) {
+              precedenceOrder.push(props.precedence);
+            }
+            if (hasHoistedHref) {
+                return (
+                  (resource.instance = instance),
+                  markNodeAsHoistable(instance),
+                  instance
+                );
+            }
             var styleProps = assign({}, props, {
               "data-href": props.href,
               "data-precedence": props.precedence,
               href: null,
               precedence: null
             });
-            instance = (
-              hoistableRoot.ownerDocument || hoistableRoot
-            ).createElement("style");
             markNodeAsHoistable(instance);
-            setInitialProperties(instance, "style", styleProps);
-            insertStylesheet(instance, props.precedence, hoistableRoot);
+            insertStyleTag(instance, styleProps, hoistableRoot);
             return (resource.instance = instance);
           case "stylesheet":
             styleProps = getStyleKey(props.href);
diff --git a/dist/compiled/react-dom-experimental/cjs/react-dom-profiling.profiling.js b/dist/compiled/react-dom-experimental/cjs/react-dom-profiling.profiling.js
index 4e90e16312e628883467a5c1699d1a3f6a37659c..fd7b7c366e5fbd353856e85d0ac3797447043e9e 100644
--- a/dist/compiled/react-dom-experimental/cjs/react-dom-profiling.profiling.js
+++ b/dist/compiled/react-dom-experimental/cjs/react-dom-profiling.profiling.js
@@ -17619,34 +17619,99 @@ function getScriptKey(src) {
 function getScriptSelectorFromKey(key) {
   return "script[async]" + key;
 }
+var insertedHrefs = new Set();
+var rulesByPrecedence = new Map();
+var precedenceOrder = [];
+var styleElement = null;
+
+function getOrCreateStyleElement() {
+  if (!styleElement) {
+    styleElement = document.createElement('style');
+    document.head.appendChild(styleElement);
+  }
+  return styleElement;
+}
+
+function getInsertionIndex(sheet, precedence) {
+  const precedenceIndex = precedenceOrder.indexOf(precedence);
+  if (precedenceIndex === -1) {
+    // New precedence goes at the end
+    return sheet.cssRules.length;
+  }
+
+  // Find the first rule index of the next precedence level
+  for (let i = precedenceIndex + 1; i < precedenceOrder.length; i++) {
+    const nextPrecedenceRules = rulesByPrecedence.get(precedenceOrder[i]);
+    if (nextPrecedenceRules?.[0]) {
+      return nextPrecedenceRules[0].ruleIndex;
+    }
+  }
+
+  // If no next precedence found, insert at the end
+  return sheet.cssRules.length;
+}
+
+function updateRuleIndices(startIndex, shift) {
+  // Update all rule indices that come after the insertion point
+  for (const rules of rulesByPrecedence.values()) {
+    for (const rule of rules) {
+      if (rule.ruleIndex >= startIndex) {
+        rule.ruleIndex += shift;
+      }
+    }
+  }
+}
+
+function insertStyleTag(el, props, hoistableRoot) {
+  const sheet = el.sheet;
+  const precedence = props['data-precedence'];
+  const href = props['data-href'];
+  const insertionIndex = getInsertionIndex(el.sheet, precedence);
+  try {
+    // Insert the new rule at the calculated index
+    const ruleIndex = sheet.insertRule(props.children, insertionIndex);
+
+    // Update indices of subsequent rules
+    updateRuleIndices(insertionIndex, 1);
+
+    // Get or create array for this precedence
+    const rulesForPrecedence = rulesByPrecedence.get(precedence) || [];
+    rulesByPrecedence.set(precedence, rulesForPrecedence);
+
+    // Add the new rule with its index
+    rulesForPrecedence.push({ href, ruleIndex });
+  } catch (error) {
+    console.error('Error inserting CSS rule:', error);
+  }
+  performance.mark('Style_end', {
+    detail: href
+  });
+}
 function acquireResource(hoistableRoot, resource, props) {
   resource.count++;
   if (null === resource.instance)
     switch (resource.type) {
       case "style":
-        var instance = hoistableRoot.querySelector(
-          'style[data-href~="' +
-            escapeSelectorAttributeValueInsideDoubleQuotes(props.href) +
-            '"]'
-        );
-        if (instance)
-          return (
-            (resource.instance = instance),
-            markNodeAsHoistable(instance),
-            instance
-          );
+        var instance = getOrCreateStyleElement();
+        var hasHoistedHref = insertedHrefs.has(props.href);
+        if (!precedenceOrder.includes(props.precedence)) {
+          precedenceOrder.push(props.precedence);
+        }
+        if (hasHoistedHref) {
+            return (
+              (resource.instance = instance),
+              markNodeAsHoistable(instance),
+              instance
+            );
+        }
         var styleProps = assign({}, props, {
           "data-href": props.href,
           "data-precedence": props.precedence,
           href: null,
           precedence: null
         });
-        instance = (hoistableRoot.ownerDocument || hoistableRoot).createElement(
-          "style"
-        );
         markNodeAsHoistable(instance);
-        setInitialProperties(instance, "style", styleProps);
-        insertStylesheet(instance, props.precedence, hoistableRoot);
+        insertStyleTag(instance, styleProps, hoistableRoot);
         return (resource.instance = instance);
       case "stylesheet":
         styleProps = getStyleKey(props.href);
diff --git a/dist/compiled/react-dom-experimental/cjs/react-dom-unstable_testing.development.js b/dist/compiled/react-dom-experimental/cjs/react-dom-unstable_testing.development.js
index bf8d758663a56e4409d29b3abebdab127ef09a0e..014521651a8734d8b055c6ed6b2b22b783e497e3 100644
--- a/dist/compiled/react-dom-experimental/cjs/react-dom-unstable_testing.development.js
+++ b/dist/compiled/react-dom-experimental/cjs/react-dom-unstable_testing.development.js
@@ -23140,34 +23140,99 @@
     function getScriptSelectorFromKey(key) {
       return "script[async]" + key;
     }
+    var insertedHrefs = new Set();
+    var rulesByPrecedence = new Map();
+    var precedenceOrder = [];
+    var styleElement = null;
+    
+    function getOrCreateStyleElement() {
+      if (!styleElement) {
+        styleElement = document.createElement('style');
+        document.head.appendChild(styleElement);
+      }
+      return styleElement;
+    }
+    
+    function getInsertionIndex(sheet, precedence) {
+      const precedenceIndex = precedenceOrder.indexOf(precedence);
+      if (precedenceIndex === -1) {
+        // New precedence goes at the end
+        return sheet.cssRules.length;
+      }
+    
+      // Find the first rule index of the next precedence level
+      for (let i = precedenceIndex + 1; i < precedenceOrder.length; i++) {
+        const nextPrecedenceRules = rulesByPrecedence.get(precedenceOrder[i]);
+        if (nextPrecedenceRules?.[0]) {
+          return nextPrecedenceRules[0].ruleIndex;
+        }
+      }
+    
+      // If no next precedence found, insert at the end
+      return sheet.cssRules.length;
+    }
+    
+    function updateRuleIndices(startIndex, shift) {
+      // Update all rule indices that come after the insertion point
+      for (const rules of rulesByPrecedence.values()) {
+        for (const rule of rules) {
+          if (rule.ruleIndex >= startIndex) {
+            rule.ruleIndex += shift;
+          }
+        }
+      }
+    }
+    
+    function insertStyleTag(el, props, hoistableRoot) {
+      const sheet = el.sheet;
+      const precedence = props['data-precedence'];
+      const href = props['data-href'];
+      const insertionIndex = getInsertionIndex(el.sheet, precedence);
+      try {
+        // Insert the new rule at the calculated index
+        const ruleIndex = sheet.insertRule(props.children, insertionIndex);
+    
+        // Update indices of subsequent rules
+        updateRuleIndices(insertionIndex, 1);
+    
+        // Get or create array for this precedence
+        const rulesForPrecedence = rulesByPrecedence.get(precedence) || [];
+        rulesByPrecedence.set(precedence, rulesForPrecedence);
+    
+        // Add the new rule with its index
+        rulesForPrecedence.push({ href, ruleIndex });
+      } catch (error) {
+        console.error('Error inserting CSS rule:', error);
+      }
+      performance.mark('Style_end', {
+        detail: href
+      });
+    }
     function acquireResource(hoistableRoot, resource, props) {
       resource.count++;
       if (null === resource.instance)
         switch (resource.type) {
           case "style":
-            var instance = hoistableRoot.querySelector(
-              'style[data-href~="' +
-                escapeSelectorAttributeValueInsideDoubleQuotes(props.href) +
-                '"]'
-            );
-            if (instance)
-              return (
-                (resource.instance = instance),
-                markNodeAsHoistable(instance),
-                instance
-              );
+            var instance = getOrCreateStyleElement();
+            var hasHoistedHref = insertedHrefs.has(props.href);
+            if (!precedenceOrder.includes(props.precedence)) {
+              precedenceOrder.push(props.precedence);
+            }
+            if (hasHoistedHref) {
+                return (
+                  (resource.instance = instance),
+                  markNodeAsHoistable(instance),
+                  instance
+                );
+            }
             var styleProps = assign({}, props, {
               "data-href": props.href,
               "data-precedence": props.precedence,
               href: null,
               precedence: null
             });
-            instance = (
-              hoistableRoot.ownerDocument || hoistableRoot
-            ).createElement("style");
             markNodeAsHoistable(instance);
-            setInitialProperties(instance, "style", styleProps);
-            insertStylesheet(instance, props.precedence, hoistableRoot);
+            insertStyleTag(instance, styleProps, hoistableRoot);
             return (resource.instance = instance);
           case "stylesheet":
             styleProps = getStyleKey(props.href);
diff --git a/dist/compiled/react-dom-experimental/cjs/react-dom-unstable_testing.production.js b/dist/compiled/react-dom-experimental/cjs/react-dom-unstable_testing.production.js
index d6c120c966ea535e247ab0b8e5b67eb462bc2fd8..a5af1f1679564c842b4ed8a96b047169f3923fae 100644
--- a/dist/compiled/react-dom-experimental/cjs/react-dom-unstable_testing.production.js
+++ b/dist/compiled/react-dom-experimental/cjs/react-dom-unstable_testing.production.js
@@ -16538,34 +16538,100 @@ function getScriptKey(src) {
 function getScriptSelectorFromKey(key) {
   return "script[async]" + key;
 }
+var insertedHrefs = new Set();
+var rulesByPrecedence = new Map();
+var precedenceOrder = [];
+var styleElement = null;
+
+function getOrCreateStyleElement() {
+  if (!styleElement) {
+    styleElement = document.createElement('style');
+    document.head.appendChild(styleElement);
+  }
+  return styleElement;
+}
+
+function getInsertionIndex(sheet, precedence) {
+  const precedenceIndex = precedenceOrder.indexOf(precedence);
+  if (precedenceIndex === -1) {
+    // New precedence goes at the end
+    return sheet.cssRules.length;
+  }
+
+  // Find the first rule index of the next precedence level
+  for (let i = precedenceIndex + 1; i < precedenceOrder.length; i++) {
+    const nextPrecedenceRules = rulesByPrecedence.get(precedenceOrder[i]);
+    if (nextPrecedenceRules?.[0]) {
+      return nextPrecedenceRules[0].ruleIndex;
+    }
+  }
+
+  // If no next precedence found, insert at the end
+  return sheet.cssRules.length;
+}
+
+function updateRuleIndices(startIndex, shift) {
+  // Update all rule indices that come after the insertion point
+  for (const rules of rulesByPrecedence.values()) {
+    for (const rule of rules) {
+      if (rule.ruleIndex >= startIndex) {
+        rule.ruleIndex += shift;
+      }
+    }
+  }
+}
+
+function insertStyleTag(el, props, hoistableRoot) {
+  const sheet = el.sheet;
+  const precedence = props['data-precedence'];
+  const href = props['data-href'];
+  const insertionIndex = getInsertionIndex(el.sheet, precedence);
+  try {
+    // Insert the new rule at the calculated index
+    const ruleIndex = sheet.insertRule(props.children, insertionIndex);
+
+    // Update indices of subsequent rules
+    updateRuleIndices(insertionIndex, 1);
+
+    // Get or create array for this precedence
+    const rulesForPrecedence = rulesByPrecedence.get(precedence) || [];
+    rulesByPrecedence.set(precedence, rulesForPrecedence);
+
+    // Add the new rule with its index
+    rulesForPrecedence.push({ href, ruleIndex });
+  } catch (error) {
+    console.error('Error inserting CSS rule:', error);
+  }
+  performance.mark('Style_end', {
+    detail: href
+  });
+}
+
 function acquireResource(hoistableRoot, resource, props) {
   resource.count++;
   if (null === resource.instance)
     switch (resource.type) {
       case "style":
-        var instance = hoistableRoot.querySelector(
-          'style[data-href~="' +
-            escapeSelectorAttributeValueInsideDoubleQuotes(props.href) +
-            '"]'
-        );
-        if (instance)
-          return (
-            (resource.instance = instance),
-            markNodeAsHoistable(instance),
-            instance
-          );
+        var instance = getOrCreateStyleElement();
+        var hasHoistedHref = insertedHrefs.has(props.href);
+        if (!precedenceOrder.includes(props.precedence)) {
+          precedenceOrder.push(props.precedence);
+        }
+        if (hasHoistedHref) {
+            return (
+              (resource.instance = instance),
+              markNodeAsHoistable(instance),
+              instance
+            );
+        }
         var styleProps = assign({}, props, {
           "data-href": props.href,
           "data-precedence": props.precedence,
           href: null,
           precedence: null
         });
-        instance = (hoistableRoot.ownerDocument || hoistableRoot).createElement(
-          "style"
-        );
         markNodeAsHoistable(instance);
-        setInitialProperties(instance, "style", styleProps);
-        insertStylesheet(instance, props.precedence, hoistableRoot);
+        insertStyleTag(instance, styleProps, hoistableRoot);
         return (resource.instance = instance);
       case "stylesheet":
         styleProps = getStyleKey(props.href);
